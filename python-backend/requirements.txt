# AI Governance MCP Server — Python Dependencies
# All libraries pinned for reproducible builds in air-gapped environments
#
# ╔══════════════════════════════════════════════════════════════════╗
# ║  NOTE: Several packages listed in documentation (evidently,     ║
# ║  aif360, etc.) are NOT imported at runtime — they are retained   ║
# ║  here as optional extras for future enhancement. The core audit  ║
# ║  engine runs on a minimal subset.                                ║
# ╚══════════════════════════════════════════════════════════════════╝

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
python-multipart==0.0.9

# Database
asyncpg==0.29.0
neo4j==5.22.0
redis[hiredis]==5.0.7

# ML Bias & Fairness (Phase 4: Bias Engine)
fairlearn==0.10.0
scikit-learn==1.5.1        # transitive via fairlearn
scipy==1.14.1              # statistical tests for drift detection

# Drift Monitoring (Phase 9: Evidently AI integration)
evidently==0.4.33
pandas==2.2.2
numpy==1.26.4

# Cryptographic signing for W3C Verifiable Credentials
cryptography==42.0.8
pyjwt==2.8.0

# Password hashing (production auth)
passlib[bcrypt]==1.7.4

# Adversarial Testing — real LLM API calls + LLM-as-judge + embedding OOD
# sentence-transformers is optional; OOD falls back to char-entropy
# sentence-transformers==3.0.1  # uncomment for embedding-based OOD

# Async messaging (Redis Streams)
# (redis is already listed above)

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0
ruff==0.5.0

# Type checking
mypy==1.10.1

# Database migrations
alembic==1.13.2
sqlalchemy[asyncio]==2.0.32
